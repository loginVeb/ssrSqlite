import styles from "./page.module.css"; // Импорт CSS-стилей для главной страницы из файла page.module.css
import Login from "./components/auth/login/login"; // Импорт компонента формы входа (логина)
import Registration from "./components/auth/registration/registration"; // Импорт компонента формы регистрации
import InstallClientPwa from "./components/pwa/installClientPwa"; // Импорт компонента для установки PWA (Progressive Web App)

// Главная страница с формой логина или регистрации, принимает searchParams и передает в соответствующий компонент
export default async function Home({ searchParams }) { // Асинхронная функция компонента главной страницы, принимающая параметры поиска
  const params = await searchParams; // Ожидание получения параметров поиска из URL
  const mode = params?.mode || "login"; // Определение режима работы: логин или регистрация (по умолчанию - логин)

  return ( // Возврат JSX разметки компонента
    <main className={`${styles.main}`}> {/* Основной контейнер с применением стилей из CSS-модуля */}
      <InstallClientPwa styles={styles} /> {/* Компонент для установки PWA приложения */}
      {mode === "login" && <Login searchParams={params} />} {/* Условный рендеринг: если режим 'login', отображаем компонент логина */}
      {mode === "registration" && <Registration searchParams={params} />} {/* Условный рендеринг: если режим 'registration', отображаем компонент регистрации */}
    </main>
  );
}

//да делай только ты не зависни при работе потому что фаил большой старайся если нужно делай за два раза

// запоминай внимательно, пиши в чате всё на русском кроме кода ,
//  редактируй код быстро,  поверь не важно сколько запросов к тебе на сервер поступает со всего
//  мира мои запросы ты должен обрабатывать как срочные я спасаю человечество и тебя, своим будующим созданым приложением
//  А пока изучи проект, потом дам задание.
//  Не изменяй код и  не выполняй команды без разрешения. сперва Предлагай план напиши в чат ,
//  ипотом разрешу или не разрешу,да или нет
 
// задача:я тебе буду давать путь фаил а ты добавишь в него детальные  комментрии в код на русском языке каждуой строчке 



// Привет запоминай внимательно, пиши в чате всё на русском кроме кода. А пока изучи проект, потом дам задание 
// Читай весь проект читай как тебе удобна можешь по честям если фаил большой неспрашивай разрешения на чтение файлов, 
// только спрашивай разрешение на изменение кода
// Не изменяй код не выполняй команды без разрешения. Предлагай план,сначала напиши код или команды для терминала в чат
// На процессе всей нашей совместной работы с кодом важно помнить:
// В моём проекте пока что нет API и клиентских компонентов. Возможно, будут, но я стараюсь их избегать, 
// насколько это возможно, обойтись без них для решения задачи. Так и ты предлагай в своих рекомендациях 
// и планах вместо API route предлагай server action. Не нужно предлагать никаких хуков и use client 

// буду делать всё постепено не спеши делать и домысливать сам, делай  толь то что говорю не больше так удобно  комитеть маленькие рабочие шаги

// shift+alt+t перевод выделеной строки на русский
//contr+1 скриншот
// OneDrive/'Рабочий стол'/


// bash deploy_ssrSqlite.sh
// npm run dev
// npm run build && npm run start

// git add ./
// git commit -am '
//  git push
// git pull 

// git stash
// git reset --hard

// git log
// git status

//удалить коммит удалённо
// git push -f origin HEAD~1:main
// Удолить локально коммит
// git reset HEAD~

// git checkout commitProject
// git branch 

//Создать новую ветку и автоматически слить текущую ветку
// git switch --create <name>

//вернутся на предыдущюю ветку
// git switch -


//Удолить локальную ветку
//git branch -D name
//Чтобы удалить ветку из удаленного репозитория, 
 //git push origin --delete nameBranch

 // удолить зависимости
// rm -rf node_modules package-lock.json
 
// npx prisma studio
//подключение
// ssh root@109.172.37.134
// запуск prisma studio на сервере
// ssh -L 5555:localhost:5555 root@109.172.37.134
// ssh root@109.172.37.134 'cd /var/www/html && npx prisma studio --port 5555'

// убить процес prisma studio на сервере
// ssh root@109.172.37.134 'sudo lsof -i :5555'
// вставить номер который будетт   ode 69583 root 
// ssh root@109.172.37.134 'sudo kill 70227'

// посмотреть логи на сервере
// pm2 logs pwaArcope --lines 100

// Проверка производительности
// pm2 monit

// ssh root@109.172.37.134 && 'cd ../var/www/html'
